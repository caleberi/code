#include <stdio.h>
#include <memory.h>
#include <stdlib.h>

const char* replace(const char* string,char* rep){
        const char* result = (char*) malloc(sizeof(string));
        int i=0;
        while (string[i]!='\0')
        {
                if (string[i+1]==' ')
                {
                        
                }
                
        }
}

int main() {
    return 0;
}