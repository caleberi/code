#include <bits/stdc++.h>

using namespace std;

int stairWaysToHeaven(int n)
{
        vector<int> ways(n+1);
        ways[0]=1;
        ways[1]=1;
        for (int i = 2; i < n+1; i++)
        {
                ways[i]=ways[i-1]+
        }
        
}

int main(int argc, char const *argv[])
{
    
        return 0;
}
